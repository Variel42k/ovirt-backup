# Сборка

Проект собирается одной командой на любой системе, где есть Go и Node. Результат
— комплект для переноса на сервер: два статических бинаря, веб-интерфейс,
конфигурация, юнит systemd и скрипт установки.

```bash
./scripts/build.sh --target linux/amd64
```

```
готово: dist/jhvirt-1.0.0-linux-amd64.tar.gz (14M)
```

---

## Что нужно

| | Версия | Зачем |
|---|---|---|
| Go | 1.26+ | бинари |
| Node | 20+ | веб-интерфейс |
| tar, gzip | любые | упаковка комплекта |

Больше ничего. **Компилятор C не нужен:** cgo не используется, драйвер SQLite —
на чистом Go. Отсюда два следствия: бинарь получается статическим и работает в
любом дистрибутиве независимо от версии libc, и кросс-компиляция возможна с
любой платформы — собрать Linux-бинарь из-под Windows это обычный путь, а не
обходной.

`make` не требуется. Makefile в репозитории есть, но скрипты сборки на него не
опираются: make отсутствует в стандартной установке Windows и в минимальных
образах CI.

---

## Как собрать

### Под текущую платформу

```bash
./scripts/build.sh
```

### Под сервер

```bash
./scripts/build.sh --target linux/amd64    # обычный сервер
./scripts/build.sh --target linux/arm64    # ARM
```

### Полезные ключи

| Ключ | Что делает |
|---|---|
| `--version 1.2.0` | явная версия вместо взятой из git |
| `--skip-web` | только бинари; `web/dist` должен быть собран заранее |
| `--no-archive` | разложить в `dist/`, не паковать в архив |
| `--help` | краткая справка |

Версия по умолчанию берётся из `git describe`. Суффикс `-dirty` означает, что в
рабочем каталоге есть несохранённые правки — он намеренно оставлен видимым,
чтобы «та самая сборка» не спуталась с похожей.

### Windows

Скрипт работает в Git Bash (идёт вместе с Git for Windows):

```bash
./scripts/build.sh --target linux/amd64
```

Из PowerShell или cmd — тем же способом через bash:

```powershell
& "C:\Program Files\Git\bin\bash.exe" scripts/build.sh --target linux/amd64
```

Права на файлы в архиве не зависят от системы сборки: NTFS не хранит бит
исполнения, поэтому бинари упаковываются отдельным проходом с явным режимом
0755. Без этого распакованный на сервере бинарь не запустился бы.

---

## Что получается

```
dist/jhvirt-1.0.0-linux-amd64/
├── bin/
│   ├── justhpc-virt-server     сервис с веб-интерфейсом
│   └── jvbackup                автономный CLI: бэкап, проверка, восстановление
├── web/dist/                   собранный интерфейс
├── config/virt-manager.yaml    конфигурация по умолчанию
├── systemd/jhvirt.service      юнит
├── docs/                       DEPLOY, OPERATIONS, README
├── install.sh                  установка и обновление
└── VERSION
```

Всё это лежит и в `dist/jhvirt-1.0.0-linux-amd64.tar.gz`.

---

## Установка из комплекта

```bash
scp dist/jhvirt-1.0.0-linux-amd64.tar.gz server:/tmp/
ssh server
tar xzf /tmp/jhvirt-1.0.0-linux-amd64.tar.gz -C /tmp
sudo /tmp/jhvirt-1.0.0-linux-amd64/install.sh
```

Установщик создаёт системного пользователя `jhvirt`, раскладывает файлы в
`/opt/jhvirt`, ставит юнит и делает `daemon-reload`. Дальше — вручную, и это
намеренно: службу он не запускает, конфигурацию не заполняет, TLS не выпускает.
Установщик, который принимает такие решения молча, хуже, чем несколько лишних
команд.

Каталог `data` получает права `0700`: в нём лежит ключ шифрования секретов.

| Команда | Что делает |
|---|---|
| `sudo ./install.sh` | установка в `/opt/jhvirt` |
| `sudo PREFIX=/srv/jhvirt ./install.sh` | другой каталог |
| `sudo ./install.sh --uninstall` | удалить службу, сохранив данные |

Дальнейшие шаги — в [DEPLOY.md](DEPLOY.md): конфигурация, TLS, чек-лист.

### Обновление

Тот же скрипт на новом комплекте. Он определит установленную версию, остановит
службу на время замены файлов и запустит обратно, если она работала:

```
==> обновление: 1.0.0 -> 1.1.0
    останавливаю службу на время замены
==> служба обновлена и запущена
```

Конфигурация не перезаписывается: новая версия кладётся рядом как
`virt-manager.yaml.new`. Данные и журналы не трогаются.

Служба останавливается намеренно, а не подменяется на ходу: заменить файл под
работающим процессом Linux позволяет, но применилось бы это только после
перезапуска — и «обновил, а версия старая» стало бы загадкой.

---

## Разработка

```bash
go run ./cmd/justhpc-virt-server -config config/virt-manager.yaml   # сервер на :8080
cd web && npm run dev                                              # интерфейс на :9000
```

В режиме разработки интерфейс живёт на своём порту и проксирует `/api` на
сервер, поэтому пересобирать SPA после каждой правки не нужно.

Перед коммитом:

```bash
gofmt -w -s $(find . -name '*.go' -not -path './web/*')
go vet ./...
go test ./...
cd web && npm run typecheck
```

Тесты не требуют внешних сервисов: база поднимается во временном каталоге,
гипервизор не нужен.

### Детектор гонок

```bash
go test -race ./...
```

Требует `CGO_ENABLED=1` и компилятора C — единственное место, где cgo нужен.
Без компилятора команда завершится сообщением `-race requires cgo`. В образе
`golang` (не `alpine`) он есть.

---

## Docker

```bash
docker build -t justhpc-virt-manager:1.0.0 .
```

Образ многоступенчатый: Node собирает интерфейс, Go — бинарь, финальный слой на
Alpine содержит только результат. Внутри `qemu-img` — он нужен для экспорта в
qcow2 и режима проверки `qemu`; без него работает всё остальное.

Полный стек с PostgreSQL и MinIO — в [DEPLOY.md](DEPLOY.md#2-docker--рекомендуемый-путь).

---

## Если что-то не собирается

| Сообщение | Причина |
|---|---|
| `не найден go` | Go не установлен или не в `PATH` |
| `не найден npm` | нет Node; либо соберите с `--skip-web`, если `web/dist` уже готов |
| `нет web/dist/index.html` | указан `--skip-web`, но интерфейс не собран |
| `-race requires cgo` | нет компилятора C; тесты без `-race` пройдут |
| `cannot execute: required file not found` при запуске `.sh` на сервере | скрипт с CRLF; в репозитории это закрыто `.gitattributes`, проверьте, чем копировали |
