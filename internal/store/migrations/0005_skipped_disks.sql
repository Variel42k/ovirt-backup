-- Что не попало в копию и почему.
--
-- Диски отбрасывались молча: общий (shareable) диск, том с ISO, диск,
-- исключённый настройкой задания. Причины были только в комментариях к коду, а
-- оператор видел «успешно, 2 диска» и не знал, что третий не сохранён.
--
-- Это самый опасный вид тишины во всей системе: неудавшийся бэкап хотя бы
-- виден как неудавшийся, а успешный с дырой выглядит как защита, которой нет.

ALTER TABLE backup_runs ADD COLUMN skipped_disks TEXT NOT NULL DEFAULT '';
