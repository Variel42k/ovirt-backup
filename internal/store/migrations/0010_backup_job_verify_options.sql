-- Параметры пробного запуска должны быть частью задания. Без них boot-проверка
-- после бэкапа могла работать только для KVM-источника, неявно используя тот
-- же хост; для oVirt выбрать отдельный KVM-гипервизор было невозможно.
ALTER TABLE backup_jobs
    ADD COLUMN verify_options JSONB NOT NULL DEFAULT '{}'::jsonb;
