/bin/
/dist/
/data/
/logs/
coverage.out
*.test

# Собранный интерфейс и зависимости
/web/dist/
/web/node_modules/
/web/.vite/
/web/test-results/
/web/playwright-report/

# Каталог, куда compose монтирует локальное хранилище копий: туда попадают
# сами бэкапы, а им в репозитории не место.
/deploy/backups/
# Конфигурация установки: её пишет установщик рядом с compose-файлом, и в ней
# соответствие групп ролям конкретной площадки. В репозитории ей не место —
# образец лежит в config/ovirt-backup.yaml.
/deploy/ovirt-backup.yaml

# Локальные переопределения конфигурации и секреты — в репозиторий не попадают
/config/virt-manager.local.yaml
*.key
.env

.idea/
.vscode/
*.swp
.DS_Store
/deploy/restores/
