-- Подсказки администратору в запуске бэкапа.
--
-- Когда служба не может сама убрать за собой на движке (закрыть бэкап,
-- отменить передачу образа), запуск сохраняет готовые команды: адрес движка,
-- идентификаторы ВМ, бэкапов и дисков уже подставлены. JSON в TEXT, как
-- skipped_disks; пусто — подсказок нет.
ALTER TABLE backup_runs ADD COLUMN IF NOT EXISTS manual_steps TEXT NOT NULL DEFAULT '';
